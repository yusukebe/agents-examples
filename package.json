{
  "name": "my-agent-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --parallel build"
  },
  "devDependencies": {
    "@babel/plugin-proposal-decorators": "^7.29.7"
  }
}
